# DEPRECATED: This file is no longer used

This tox.ini file referenced the old monolithic structure which has been removed.

The proper tox.ini file for agoras v2.0 is located at:
  packages/tox.ini

To run tests with tox:
  cd packages && tox -e all

Available tox environments:
  - tox -e all        # Test all packages
  - tox -e coverage   # Generate coverage report
  - tox -e lint       # Lint all packages

For more information, see packages/README.md
